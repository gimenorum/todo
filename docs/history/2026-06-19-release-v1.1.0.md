# 2026-06-19 Phase 6 リリース（v1.1.0）

## 日付
2026-06-19

## 依頼内容
- develop に揃った Phase 6 の機能と Issue #63 の修正を、リリースまで一気に進めたい。

## 対応概要
- バージョンを `1.0.0` → **`1.1.0`** に更新（`package.json`）。新機能を含む feature リリースのため minor 上げ。
  main へマージされると `release.yml` がタグ `v1.1.0` を作成し、正式リリース（major≥1）として GitHub Release を公開、Pages へデプロイする。
- 今回のリリースに含む変更（develop 上で先行マージ済み）:
  - #64 タスクの手動並べ替え（ドラッグ&ドロップ）＋一覧の並び替え（5択）・絞り込み（4軸）（Phase 6）
  - #65/#66 タグ欄をチップ式入力に刷新＋既存タグからの候補サジェスト
  - #63 IndexedDB オープンのハングで初期表示が「読み込み中…」固着する問題を修正
- リリース手順: `feature/release-v1.1.0`（version bump）→ develop へ PR マージ → `develop → main` の PR でリリース。

## 決定事項
- バージョン v1.1.0（ユーザー確認済み）。Phase 6 の機能群＋#63 修正をまとめて出す。
- Git Flow は v1.0.0 と同じ（release ブランチ → develop → main）。タグ/Release/デプロイは `release.yml` 任せ。

## 成果物
- 変更: `package.json`（version 1.1.0）
- 追加: 本履歴ファイル
