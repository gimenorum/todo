# 2026-06-19 README（ユーザーマニュアル）整備

## 日付
2026-06-19

## 依頼内容
- README を記載し、一般ユーザが迷わない文言の**ユーザーマニュアル**として運用する（Issue #58）。
- Phase 5（v1.0.0）リリースに含めたい。エラー自動起票（#57）とは別 PR。

## 対応概要
- リポジトリ直下に `README.md` を新規作成（日本語・一般ユーザ向け）。
- 専門用語（JSON/マージ/tombstone/DAG 等）を避け、**UI の実文言と表記を一致**させた:
  - ナビ「タスク」「設定」、設定の「クラウド連携」「同期設定」「データ」「アプリ」。
  - 連携（Dropbox/Google Drive と連携・連携を解除・再連携）、同期設定（手動のみ/自動（間隔）/同期間隔（分）/今すぐ同期）。
  - 競合（「要解決」→「同期エラーを解決」→ オンライン/保留中/直接入力、編集版を残す/削除を適用、プレビュー/確定する）。
  - データ（タスクをバックアップ/タスク (Markdown)/タスク (CSV)/設定をバックアップ/全体をバックアップ、
    バックアップから読み込む、ローカルデータを削除/クラウドから復元/連携を解除してすべて削除）。
  - インストール導線（PC/iOS/Android）、プライバシー（BYOS）、開発者向け（npm スクリプト＋docs リンク）。

## 決定事項
- ユーザーマニュアルとして継続運用（機能追加時に更新）。開発者向けは末尾に簡潔に＋`docs/` へリンク。
- バージョン据え置き（0.4.11）。develop 向け PR。

## 成果物
- 追加: `README.md`
- 追加: 本履歴ファイル
- 検証: `npm run lint` 緑（md は対象外）。文言は実装済み UI と突き合わせ済み。
