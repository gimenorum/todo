# 2026-06-17 TODO PWA 要件定義の追加とブランチ再編

## 日付
2026-06-17

## 依頼内容
- TODO PWA（ローカルファースト＋Bring Your Own Storage）の実装依頼内容を「要件定義」としてファイルに保存し、`main` ブランチにコミットすること。
- あわせて、本セッションの作業を証跡として `docs/history` に残すこと。
- 今後は、履歴の証跡を作業（成果物）と同じコミットに含めること（運用ルールの変更）。

## 対応概要
- 要件定義を `docs/requirements.md` として全文保存し、`main` にコミット（`297dc34`）・push。
- （本セッション前半）ブランチ再編：
  - 空リポジトリへの初回 push で作業ブランチ `claude/blissful-lamport-6pjsj4` が既定ブランチに自動設定されていた。
  - `main` を「空コミット（`6445668`）→ ファイル追加（`be0cb20`）」の2コミット構成で新設・push。
  - ユーザーが GitHub 設定で既定ブランチを `main` に変更。
  - 旧デフォルト `claude/blissful-lamport-6pjsj4` はローカル削除済み。リモートは環境プロキシの制約（ref 削除が 403）で削除できず、GitHub 画面からの手動削除待ち。
- CLAUDE.md の証跡コミット方針を「履歴は作業と同じコミットに含める（単独コミットにしない）」に更新。

## 決定事項
- 要件定義の保存先・ファイル名は `docs/requirements.md`。
- 今後、履歴ファイルはその作業の成果物と **同一コミット** に含める（履歴だけの単独コミットは作らない）。
- 旧デフォルトブランチのリモート削除は、環境制約のため GitHub 画面で手動対応する。

## 成果物
- `docs/requirements.md`（新規・別コミット `297dc34`）
- `docs/history/2026-06-17-requirements-definition.md`（新規・本コミット）
- `CLAUDE.md`（更新：証跡コミット方針・本コミット）
